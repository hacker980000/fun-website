<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Production trusts platform CAs only and never permits cleartext traffic. -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Debug builds may opt into a user-installed CA for local inspection. This
         debug override is ignored by non-debuggable release builds. -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
